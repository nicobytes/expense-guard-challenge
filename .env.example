# Expense Guard routes model calls through the Vercel AI Gateway (bare gateway model
# ids like "anthropic/claude-...") — provide a gateway key so `eve dev` / `eve eval`
# can reach a model:
AI_GATEWAY_API_KEY=

# Override the fixture the agent reviews in dev / eval (defaults to fixtures/request.json):
# POC_REQUEST_FILE=fixtures/ambiguous.json

# Override the host of the EVE API (defaults to http://127.0.0.1:2000):
EVE_HOST=http://127.0.0.1:2000